# DESIGN.md — الاتجاه البصري لصفحة هبوط «فصول»

> **كيف وصلنا هنا:** أربعة اتجاهات بصرية مستقلّة، حكّم كلَّ واحدٍ منها **ثلاثة محكّمين** بعدسات مختلفة (الطباعة العربية · الفخامة السينمائية · الجدوى التقنية). الفائز: **«الحِبْرُ والورق» ٧٫٣/١٠**، ثم «مِسْطَرَة الصانع» ٧٫٢، «العُمق والضوء» ٦٫٨، «الهدوء المطلق» ٦٫٧.
> ثم رُكّب الفائز: أُصلح **أحد عشر عيباً قاتلاً**، وطُعّم بـ**سبع جواهر** من الخاسرين.
> **وكل رقمٍ هنا مقيسٌ من ملفاتك، لا من الذاكرة** — قِستُ خطوطك بـ`fontTools` وتحقّقت من التباينات بحساب WCAG.

---

## ٠ — الأطروحة

**الصفحة عيّنةٌ من مُخرَج التطبيق.**

تطبيقك يُخرج وثائق رسمية: كشوف درجات، أوراق اختبار، ملفّ إنجاز بغلاف وفهرس. فتكون الصفحة نفسها **وثيقةً فاخرةً مصوَّرةً في ضوءٍ خافت**: هوامشُها مرسومة بخطٍّ شعري، وترقيمُ صفحاتها في الهامش (`٠٣ / ٠٧`)، وإيقاعُها الطباعي لا يُكسَر مرّة واحدة.

**الفخامة من الانضباط لا من الأثر.** لا صورة خلفية، لا تدرّجات صاخبة، لا ذهبٌ منثور.

> **وتعديلٌ فرضه المحكّمون الثلاثة:** أكبر خطرٍ على هذا الاتجاه هو **البرود** — «مؤسّسيٌّ باردٌ لا منتَجٌ يُحَبّ». فالدفء صار قراراً في الاتجاه نفسه بثلاث رافعات:
> 1. **النص يخاطبك ولا يصفك** — ضمير المخاطب في الصفحة كلها.
> 2. **الورقة تُضاء لا تُحدَّد** — رُفع فرقها عن الأرضية من ١٫٠٧:١ إلى **١٫١٣:١**، فتُقرأ سطحاً ولو ذابت الخطوط.
> 3. **اللقطات الأربع ملوّنةٌ وغنيّة إلزاماً** — هي مصدر الدفء الأكبر.

---

## ١ — لوحة الألوان

### الأسطح
| الرمز | القيمة | الدور |
|---|---|---|
| `--page-void` | `#090C11` | أعمق عتمة — الهيرو والفوتر وحدهما. **ميلٌ كحليّ (B>R) يمنع halation** |
| `--page` | `#0D1117` | أرضية الصفحة — **هي نفسها أرضية الوضع الداكن في تطبيقك**، فلا ينكسر الرباط |
| `--sheet` | `#171E2B` | «الورقة»: البطاقات، إطار الجوال، الـBento |
| `--sheet-raised` | `#1F2838` | الورقة المرفوعة: hover الـBento |
| `--sheet-petrol` | `#0F2A33` | ورقةٌ بميلٍ بترولي — **قسمٌ واحدٌ فقط** (الأرقام) |

### الخطوط
| الرمز | القيمة | الدور |
|---|---|---|
| `--rule-hair` | `#303D52` | الخطّ الشعري ١px: الهوامش، نهايات الأقسام (١٫٧٣:١) |
| `--rule` | `#42526C` | الفاصل الحامل: أعلى البطاقات، إطار الجوال (٢٫٣٩:١) |
| `--rule-gold` | `#897548` | **الحدّ الوحيد الحامل للمعنى** (٣٫٣١:١ في أسوأ سطح) |

### الحبر
| الرمز | القيمة | الدور |
|---|---|---|
| `--ink` | `#E8ECF3` | أبيضُ مكسورٌ لا خالص — **منقولٌ حرفياً من `theme-dark.css:41`** |
| `--ink-muted` | `#A3AEC0` | الفقرات، وصف الميزات، الفوتر |
| `--ink-faint` | `#96A2B8` | حبر الهامش: الفوليو، التسميات ١١px |

### اللون
| الرمز | القيمة | الدور |
|---|---|---|
| `--petrol` | `#0A3F4A` | الهوية — سطحاً صلباً في موضعين، ومصدراً للتوهّج في كل مكان |
| `--petrol-light` | `#14606F` | **للتوهّج فقط** (٢٫٦٤:١) — ممنوعٌ نصّاً أو أيقونةً منعاً باتّاً |
| `--petrol-deep` | `#05262D` | نصّ زرّ الـCTA فوق الذهبي (٧٫٠٦:١) |
| `--gold` | `#C9A961` | **سبعة مواضع، لا ثامن** |
| `--gold-light` | `#E8D9A8` | أرقام الإحصاءات وحدها (١٣٫٤٥:١) |

### التباين — محسوباً على كل سطح
| النص | void | page | sheet | raised | petrol-sheet |
|---|---|---|---|---|---|
| `--ink` | ١٦٫٥٣ | ١٥٫٩٧ | ١٤٫١٠ | ١٢٫٤٩ | ١٢٫٦٧ |
| `--ink-muted` | ٨٫٧٤ | ٨٫٤٥ | ٧٫٤٦ | ٦٫٦١ | ٦٫٧٠ |
| `--ink-faint` | ٧٫٦١ | ٧٫٣٥ | ٦٫٤٩ | **٥٫٧٥** | ٥٫٨٣ |
| `--gold` | ٨٫٧٠ | ٨٫٤١ | ٧٫٤٢ | ٦٫٥٨ | ٦٫٦٧ |
| `--gold-light` | ١٣٫٩٢ | ١٣٫٤٥ | ١١٫٨٧ | ١٠٫٥٢ | ١٠٫٦٦ |

> **أدنى نصٍّ في الصفحة = ٥٫٧٥:١**، وكل نصٍّ أساسيّ **≥٧:١** — نفس الحدّ الذي فرضه تطبيقك على لون المعلّم المختار.

`--gold` على البترولي = **٥٫١٢:١** ⇒ مسموحٌ عند **≥21px فقط**.

---

## ٢ — سلّم الطباعة

**التتبّع = ٠ في كل سطرٍ عربيٍّ بلا استثناء. لا سالبَ ولا موجب.**

| الدرجة | جوال ٣٧٥ | مكتب ١٤٤٠ | الوزن | ارتفاع السطر |
|---|---|---|---|---|
| hero | ٤٠px | ١٠٤px `clamp(40px,7.2vw,118px)` | ٧٠٠ | **١٫٢٠** |
| display | ٣٠px | ٦٨px | ٦٠٠ | ١٫٢٤ |
| h1 | ٢٦px | ٤٢px | ٧٠٠ | ١٫٢٤ |
| h2 | ٢١px | ٣٤px | ٦٠٠ | ١٫٢٨ |
| h3 | ١٨px | ٢١px | ٦٠٠ | ١٫٣٥ |
| body-lg | ١٧px | ٢١px | ٤٠٠ | ١٫٦١٨ (φ) |
| body | ١٥px | ١٦px | ٤٠٠ | ١٫٦١٨ |
| label | ١٣px | ١٣px | ٥٠٠ | ١٫٤٠ |
| folio | ١١px | ١١px | ٥٠٠ | **أرقامٌ ورموزٌ حصراً — لا كلمات** |
| stat | ٥٥px | ٨٩px | ٧٠٠ | ١٫١٠ |

**قاعدة صلبة:** لا تنزل العربية تحت ١٣px أبداً — النقاط والهمزات تندثر.

### كيف يصير العنوان ضخماً بوزن ٧٠٠ وحده
سبع رافعات: الحجم (٦٫٥× حجم النص) · ارتفاع السطر ١٫٢٠ فتلتحم الأسطر كتلةً · قِصَر السطر (`max-width: 14ch`… **بل `em`**، انظر أدناه) · القفزة من ٤٠٠ إلى ٧٠٠ في الجوار · العنوان وحده على `--ink` وما حوله مكتوم · الخط الذهبي تحت الكلمة المفتاح يعمل عمل «الوزن التاسع» · والفراغ ٢٣٣px فوقه و١٤٤px تحته.

### قفل العربية
```css
html { font-synthesis: none; font-synthesis-weight: none; }
:lang(ar), [dir="rtl"] { letter-spacing: 0; }
.hero__title { word-spacing: -0.015em; }   /* الكثافة من بين الكلمات لا داخلها */
/* text-align: justify — محظور: العربية بلا كشيدة تتمدّد بفجوات */
```

> **`font-synthesis: none` يحوّل درسَك في `main.css:102` من تعليقٍ إلى قاعدةٍ يفرضها المتصفّح.** لو تسرّب طلبُ ٨٠٠ من أي مكوّن، سقط إلى ٧٠٠ الحقيقي بدل أن يُمَطّ.

---

## ٣ — ستّة أخطاء قاتلة اكتُشفت بقياس خطوطك أنت

قِستُ `plex-700-arabic.woff2` و`plex-400-arabic.woff2` بـ`fontTools`. النتائج غيّرت التصميم:

| # | ما كان مقترحاً | ما كشفه القياس | الإصلاح |
|---|---|---|---|
| ١ | ارتفاع سطر **١٫٠٥** للهيرو | ي (−٤٥٥) فوق أ (٩٣٤) = **١٣٨٩ وحدة** في صندوقٍ سعتُه ١٠٥٠ ⇒ **تصادم ٣٣٩ وحدة ≈ ٣٥px** عند ١٠٤px | **١٫٢٠** |
| ٢ | `overflow: hidden` على أسطر الهيرو | صندوق الحبر ينزل ١٫٧٢٩em والصندوق ١٫٥em ⇒ **قصٌّ دائم** لهمزة أ وذيل ي | **حُذف كلياً** + `padding-block-end: 0.19em` |
| ٣ | `tabular-nums` للأرقام | **الخط لا يملك `tnum` إطلاقاً.** سماته: `calt ccmp fina init kern locl mark medi mkmk rlig`. وعروض الأرقام الهندية ٢٨١→٦٣٥ = **مدى ٢٫٢٦×** | جدولةٌ بالبناء: كل رقم في `<span>` بـ`width:.635em; text-align:center` |
| ٤ | `min-width` بوحدة `ch` | **U+0030 غير موجود في الفرعية العربية أصلاً** ⇒ `ch` يُقاس على الوجه اللاتيني | أُلغيت `ch` من الأرقام كلها |
| ٥ | `unicode-bidi: plaintext` | يشتقّ الاتجاه من أول محرفٍ قوي ⇒ «iOS ١٧ فأحدث» تقلب الفقرة إلى LTR | **`<bdi>`** حصراً |
| ٦ | الحدّ الذهبي `#746540` | **٢٫٧٩:١** على الورقة المرفوعة ⇒ يسقط عن حدّ ٣:١ | **`#897548`** — أسوأ حالة ٣٫٣١:١ |

---

## ٤ — المسافات والإيقاع

**السلّم (فيبوناتشي، بكسل):** `3 · 5 · 8 · 13 · 21 · 34 · 55 · 89 · 144 · 233`. **لا قيمة خارجه أبداً.**

- ٣ · ٥ · ٨ · ١٣ → داخليٌّ سطري
- ٢١ · ٣٤ · ٥٥ → داخل البطاقات والفجوات
- ٨٩ · ١٤٤ · ٢٣٣ → إيقاع الأقسام وحده

**حشو الأقسام:** ٢٣٣px ≥١٢٠٠ · ١٤٤px ٩٠٠–١١٩٩ · ٨٩px <٩٠٠.

**الفاصل بين قسمٍ وقسم ليس فراغاً:** خطٌّ شعريّ ١px بعرض عمود المحتوى + رقم القسم في الهامش.

**هوامش الورق — التوقيع:** خطّان رأسيّان يتبعان **عمود المحتوى** لا إطار العرض (وإلا صارا خطّين ملصقين بالحافة على شاشة ١٩٢٠). على <٩٠٠ يبقى الأيمن وحده.

**عمود المحتوى:** `max-width: 1120px`. سطر النص ≤38em، والفقرة تحت العنوان ≤28em — **بالـ`em` لا `ch`** (السبب في §٣ بند ٤).

**نقاط الكسر:** ٣٧٥ · ٦٠٠ · ٩٠٠ · ١٢٠٠ · ١٦٠٠ — خمسٌ، كلها `min-width`.

---

## ٥ — فلسفة الحركة

### المنحنيات
| الرمز | القيمة | الدور |
|---|---|---|
| `--ease-page` | `cubic-bezier(.16,1,.3,1)` | الرافعة الأساسية — **ورقةٌ تُوضع على طاولة** |
| `--ease-ink` | `cubic-bezier(.22,.61,.36,1)` | العتامة والتمويه — أنعم بدايةً فلا يُحسّ القفز |
| `--ease-rule` | `cubic-bezier(.65,0,.35,1)` | **رسم الخطوط** — متماثلٌ كقلمٍ يُجَرّ على مسطرة. توقيع الاتجاه |
| `--ease-lift` | `cubic-bezier(.34,1.12,.64,1)` | تجاوزٌ ١٢٪ لـhover الـBento **وحده** — ممنوع في الدخول |
| `--ease-ui` | `cubic-bezier(.4,0,.2,1)` | **منحنى تطبيقك نفسه** — للتفاعلات ≤233ms فلا تنفصل الصفحة عن المنتَج |

### المدد (فيبوناتشي، ms)
١٤٤ hover · ٢٣٣ تركيز · ٣٧٧ تبديل تسمية · ٦١٠ رسم خط · **٧٢٠** كلمة الهيرو · **٨٣٠** ظهور قسم · **٩٨٧** رسم الهوامش · **١١٣٠** العدّاد.
كل الكبيرة داخل **٠٫٦–١٫٢ ثانية** كما طلبت. التدرّج: ٩٠ms مكتباً · ٦٠ms جوالاً.

> **على الجوال لا تُقصَّر المدد — تُقصَّر المسافات.** كل إزاحة ١٨–٢٤px تصير ٨px.
> الزمن الأقصر يُحسّ **عصبياً**؛ المسافة الأقصر تُحسّ **هادئة**.

### التوهّج — ضوءٌ يقع على ورق، لا نيون
ثلاثة في الصفحة كلها، كلها `radial-gradient` مطليٌّ في خلفية القسم.
**صفر `filter: blur()`. صفر `background-attachment: fixed`. ولا تحريكَ لـ`box-shadow` أبداً** — الظلّ مطليٌّ سلفاً على `::after` وتُبدَّل عتامته.

> **قاعدة الذهب الفيزيائية:** الذهبي **يعكس ولا يشعّ** — `border-block-start` فقط، لا سفليَّ ولا جانبيّاً ولا توهّجاً. وهي تحصّن نفسها: العلويُّ والسفليُّ لا ينقلبان في RTL.

### الحبيبات
بلاطة **PNG ١٢٨×١٢٨ مخبوزة وقت البناء** (≈4.8KB base64، صفر طلبات) — لا `feTurbulence` وقت التشغيل. تُطلى **في خلفية القسم لا في طبقةٍ عائمة**. **لا `mix-blend-mode`** (يجبر المتصفح على قراءة الخلفية ومزجها في كل إطار). لا حركة. ولا تُحجَّم مع كثافة البكسل — تُقرأ حريراً على ٣× لا رملاً.

> **والمبرّر الأقوى ليس «إحساس الفيلم» بل كسر التشريط:** التدرّج البترولي الواسع على أرضيةٍ شبه سوداء يُظهر حلقات banding على لوحة ٨-بت. حبيبةُ ±٤ درجات تُذهبها. **فائدةٌ مقيسة تبقى ولو اختلفنا في الذوق.**

---

## ٦ — الأقسام السبعة

**١ — الهيرو** · `100svh`. الكتلة في **المركز البصري (≈٤٢٪ من الأعلى)** لا الثلث السفلي، فيصير الفراغ محتضِناً لجسمٍ مضيء لا محيطاً بعدم. الكلمة الواحدة: `opacity 0→1` + `blur(10px)→0` + `translateY(18px)→0` · ٧٢٠ms · تدرّج ٩٠ms **بترتيب RTL، الكلمة اليمنى أولاً**. **حركة CSS خالصة، صفر JS** — لو انتظرت الكلماتُ الترطيبَ لتأخّر LCP. مؤشّر تمرير رأسيّ وسط الأسفل (فلا اتجاه له).

**٢ — الجملة التعريفية** · سطران يتلوّنان مع السكرول بـ`background-clip: text` وتدرّجٍ **`to left`** — يزحف مع القراءة لا ضدّها.

**٣ — التمرير المثبّت (قلب الصفحة)** · `height: calc(4 * 100svh)` و`sticky`. **`svh` لا `vh`** — شريط Safari المتحرك يجعل `100vh` أطول من الشاشة فيقفز التثبيت. النصّ يمين والجوال يسار **بترتيب الـDOM لا `row-reverse`**، فينطبق مسار العين على مسار الـTab. الميزات المخفية بـ`visibility: hidden` لا بالعتامة وحدها، فلا يقرؤها قارئ الشاشة.

**٤ — Bento** · `grid-template-areas` لا أرقام أعمدة، والخلية الكبرى **أول عنصرٍ في الـDOM** فتقع يميناً تلقائياً. hover مغلَّفٌ بـ`@media (hover:hover)`. **بطاقة واحدة من ستّ** تحمل الحدّ الذهبي.

**٥ — الأرقام** · السطح الوحيد `--sheet-petrol`. **العدّاد يُكتب بقيمته النهائية في الـHTML الساكن** والـJS يستبدله فقط عند السماح بالحركة — يحلّ ثلاثاً دفعةً: صفر CLS، وتقليل الحركة بلا فرقٍ بصري، وصحّة الرقم بلا JS أصلاً.

**٦ — CTA الأخير** · `--page-void`. شارتا متجرين **مرسومتان بأسلوبنا** معطّلتان بـ«قريباً». **الشارات لا تُقلَب** — الشعارات لا تُعكَس أبداً.

**٧ — الفوتر** · `--page-void`. الشعار + سطر + الحقوق.

### انضباط الذهب — سبعةٌ مُحصاةٌ بالاسم
(١) حلقة الشعار ٢٫٥ · (٢) خطٌّ ١px تحت الكلمة المفتاح بعرضها تماماً · (٣) **زرّ الـCTA — المساحة الذهبية الوحيدة** · (٤) المؤشّر النشط في سكّة التمرير ٢px×٢١px · (٥) الحدّ العلوي لبطاقة Bento واحدة · (٦) الشرطتان في قسم الأرقام · (٧) `focus-visible`.

**وأربع قواعد تحرس الندرة — مكتوبةً كوداً لا نيّة:** لا ذهبيَّ تحت ٢١px · لا يجتمع ذهبيّان في إطار العرض الواحد · `--gold` ممنوعٌ داخل أي `border` عام (وقاعدة `grep` في CI تحرسها) · وموضعٌ ذهبيٌّ واحدٌ يبقى حيّاً تحت ٦٤٠px.

---

## ٧ — RTL: تحصينٌ بالبنية لا بالمراجعة

- **الخصائص المنطقية إلزامية** (`ps-/pe-`, `inset-inline`, `border-inline-*`)، و`left/right/pl/pr` **محظورة بقاعدة ESLint** + حارس `grep` في CI.
- **الفخّ الأكبر: التحويلات لا تنقلب.** `dir="rtl"` لا يمسّ `transform` بحرف. لذلك **كل حركة دخولٍ رأسيةٌ حصراً، ولا `translateX` في أي ظهور**. هذا هو الموضع الوحيد الذي ينكسر فيه RTL **صامتاً** حتى الإطلاق.
- **الأيقونات ثلاث فئات:** تُقلَب (السهم، الشيفرون) · **لا تُقلَب أبداً** (الشعار، قبّعة التخرّج، ✓، الساعة، شارتا المتجرين) · متماثلة. **والقلب مرّةً في المصدر بمسارٍ جديدٍ محفوظ، لا `scaleX(-1)` وقت التشغيل** — القلب الحيّ يعكس نهايات الحدّ المستديرة فيختلّ الوزن البصري.
- **الـparallax رأسيٌّ حصراً** (≤55px). الأفقيّ **محظور** — في RTL يُقرأ الانزلاق الأفقي مقاومةً لاتجاه القراءة.
- التدرّجات: ممنوع `to right`، تُكتب `to left` صراحةً.

---

## ٨ — ميزانية الأداء

| البند | gz |
|---|---|
| أرضية Next 14 App Router | ~87KB |
| شيفرتنا (عدّاد + مراقب + فوليو) | ~5KB |
| Lenis (مكتب فقط، **استيرادٌ مشروط**) | +3KB |
| **الإجمالي على الجوال** | **~92KB** |

**الخطوط — أكبر مكسبٍ على الصفحة:** ملفاتك اليوم ٤٧٫٤ + ٤٧٫٨ = **٩٥٫٣KB**. بتجزئةٍ خاصةٍ بمحارف الصفحة (~١٢٠ شكلاً) تهبط إلى **~20KB**، **ولا يُسبَق تحميلُ اللاتيني إطلاقاً** (٢٨KB في صفحةٍ لا لاتينيَّ فيها إلا «iOS» و«Android»).

**المتوقَّع (Lighthouse موبايل، Moto G4 ×4):** FCP ≈0.9s · **LCP ≈1.3s** · TBT ≈120ms · **CLS 0.00** · **الأداء ٩٣–٩٦**.

**بوّابات CI:** `performance ≥ 0.92` · `CLS ≤ 0.02` · حجم أول حزمة **≤110KB** · `grep` على `font-weight: 800|900|bolder` · `grep` على أصناف Tailwind الفيزيائية · فحص خلوص العنوان · فحص تركيب الحبيبات.

---

## ٩ — تقليل الحركة: مسارٌ ثانٍ كامل

كلمات الهيرو → تلاشي عتامةٍ ٢٠٠ms بلا تمويه · الخلفية → ساكنة · القسم المثبّت → `.no-pin` (أربع كتلٍ متتالية) · الـparallax → صفر مطلق · العدّاد → القيمة موجودةٌ في الـHTML أصلاً فلا شيء يُفعل.

> **ولا تُصفَّر المدد.** التبديل الفوري الحادّ يُقرأ **عطلاً**. وتُكتب القاعدة `animation-duration: 1ms !important` لا `none` — كي تُطلَق أحداث `animationend` التي يعتمد عليها نزع `will-change`.

**خفض الجوال ≤600px:** التمويه **يُحذف كلياً** (أغلى شيءٍ على معالجٍ متوسط) · التدرّج ٩٠→٦٠ms · الـparallax **يُلغى لا يُخفَّف** · الحبيبات ٣٫٥٪→٢٫٥٪ · الهامش الأيسر يختفي · **والقسم المثبّت يبقى** (هو قلب الصفحة) لكن يسقط منه `scale`.

---

## ١٠ — ما ينتظر قرارك

| # | القرار | التوصية |
|---|---|---|
| **١** | **نصّ عنوان الهيرو.** المقترح: «أوراقُ فصلِك / مرتَّبةٌ كما تحبّ / ومطبوعةٌ كما يجب» — قِستُه على خطّك فوجدتُه سليماً عند ١٫٢٠ بخلوصٍ **+١٦px** في أضيق زوج. الكلمة المفتاح للخط الذهبي: **«أوراقُ»** | أوافق عليه — وأي تغييرٍ يستدعي إعادة قياس (البوّابة تفعلها آلياً) |
| **٢** | **أرقام القسم الخامس.** لا مستخدمين بعد ⇒ لا أرقام تبنٍّ | **قدرات منتَج**: ١٩ استراتيجية · ٣٠ مبادرة · ١٠ أقسام · تقويمان رسميّان |
| **٣** | ⚠️ **إسقاط Framer Motion** لصالح `animation-timeline: scroll()/view()` | **يوفّر ١٨KB ويُخرج حركة التمرير من الخيط الرئيسي مجّاناً.** ثمنه: متصفّحٌ قديم يرى أربع كتلٍ بدل التثبيت — وهو نفس المسار المكتوب أصلاً لـreduced-motion، فصفر شيفرةٍ إضافية. **لكنّ بريفك نصّ على Framer Motion — أحتاج نقضك صراحةً** |
| **٤** | بوّابة الحزمة **١١٠KB** لا ١٠٠ | الأرضية وحدها ٨٧KB؛ بوّابة ١٠٠ تكسر البناء في أول كوميت |
| **٥** | **اللقطات الأربع** من `reports` · `portfolio` · `class-exams` · `schedule` **حصراً** | هذه الشاشات وحدها تحوّل الأرقام للهندي، فتتّسق مع أرقام الصفحة |

---

## ملحق: ثلاثة أعطال في التطبيق نفسه — كشفها التدقيق وتحقّقتُ منها

خارج نطاق الصفحة، لكنها في مستودعك الآن. **قِستُها كلَّها بنفسي:**

1. **`css/views.css:318` و`:1613` و`:1725`** — `letter-spacing` **سالبٌ على نصٍّ عربي** (`-0.1px` و`-.2px` ×٢). السالب يشبك النقاط بالأذيال في خطٍّ متّصل. ✅ مؤكَّد بـgrep.

2. **`js/print-students.js:573`** — `avg.toFixed(1)` يُخرج **رقماً لاتينياً** بينما بقية التقارير هندية. ✅ مؤكَّد بالقراءة.

3. **`font-variant-numeric: tabular-nums` في ١١ موضعاً بـ`views.css`** — **لا يفعل شيئاً على الأرقام الهندية.** قِستُ الخط: سماته `calt ccmp fina init kern locl mark medi mkmk rlig` — **`tnum` غير موجود**. وعروض الأرقام الهندية في وزن ٧٠٠: `٠`=٢٩٦ · `١`=٢٨١ · `٣`=**٦٣٥** — **مدى ٢٫٢٦×**.
   ⇒ **كل عمودٍ رقميٍّ هنديٍّ في تطبيقك غير مصطفٍّ اليوم** (كشوف الدرجات، التقارير، ملفّ الإنجاز).

> هذه الثلاثة **لم أعدّلها** — لم تطلب. أخبرني إن أردتها في مهمّة منفصلة.

---

*٣ سبتمبر ٢٠٢٦ · لم يُعدَّل أي ملف من التطبيق.*
