# LANDING_BRIEF — صفحة هبوط «فصول»

> **كيف استُخرج هذا الملف:** ٧٩ وكيلاً قرؤوا الكود فعلياً (لا التوثيق)، استخرجوا ٧٢ ميزة، ثم تحقّق منها وكلاء **عدائيّون** مهمّتهم أن يدحضوا لا أن يؤكّدوا. كل سطر أدناه مسنودٌ بملف ورقم سطر.
> **لم يُعدَّل أي ملف من التطبيق.**

---

## ⚠️ قبل كل شيء — تنبيهان يحتاجان قرارك

### ١) المشروع ليس Next.js

طلبتَ Next.js 14 + TypeScript + Tailwind + Framer Motion + Lenis. المشروع الحالي:

| | |
|---|---|
| التقنية | HTML / CSS / JavaScript **خام** — بلا أطر عمل، بلا `package.json`، بلا `node_modules` |
| البناء | لا يوجد — الملفات تُرفع كما هي |
| النشر | GitHub Pages، والـworkflow يرفع مجلّد `teacher_app` **وحده** كمُخرَج (`.github/workflows/deploy.yml:44`) |
| النطاق | `fusooli.com` مسجَّل ومُوثَّق، **وجذره فارغ** اليوم (صفحة ركن) |

**اقتراحي:** مجلّد `landing/` مستقلٌّ تماماً في جذر المستودع، بـNext.js 14 و`output: 'export'` (تصدير ساكن). لا يلمس `teacher_app` بحرف. وله ثلاثة مسارات نشر:

| الخيار | الوجهة | الكلفة | ملاحظة |
|---|---|---|---|
| **أ (مُرجَّح)** | Vercel على `fusooli.com` | مجاني | أسهل، والنطاق جاهز، والتطبيق يبقى على GitHub Pages بلا مساس |
| ب | GitHub Pages على `/landing` | مجاني | يحتاج تعديل `deploy.yml` — أي لمس بنية النشر الحالية |
| ج | نطاق فرعي `www.fusooli.com` | مجاني | يفصل الصفحة عن التطبيق فصلاً تاماً |

**قرارك مطلوب** — وسأمضي بـ«أ» إن لم تختر غيره.

### ٢) لا توجد لقطة شاشة واحدة في المشروع

مسحتُ المشروع كلَّه: **صفر** ملفات `png/jpg/svg/webp/ico`، ولا `manifest`، ولا `favicon`. الهوية البصرية كلّها SVG مكتوب داخل الترميز.

طلبتَ في القسم الثالث «لقطات فعلية من شاشات التطبيق». الحل: **أشغّل التطبيق محلياً وألتقط لقطات حقيقية** من الشاشات (الرئيسية، سجل المتابعة، الجدول، ملف الإنجاز، ورقة الاختبار) وأضعها في موك أب الجوال. أخبرني إن وافقت.

---

## ١) وصفٌ بجملة واحدة

> **«فصول» — تطبيق سعوديّ يجمع سجلَّ طلابك وجدولَك واختباراتِك وملفَّ إنجازك في مكانٍ واحد، يعمل بلا إنترنت، ويُخرج كلَّ ذلك أوراقاً رسميةً جاهزةً للطباعة.**

والوصف المنشور في التطبيق نفسه (`teacher_app/index.html:6`):
> «فصول — سجل الطلاب والاختبارات وملف الإنجاز»

وجملة شاشة الدخول (`js/views/login.js:29`):
> «فصولك وطلابك وحضورهم ودرجاتهم في مكان واحد»

---

## ٢) الجمهور المستهدف — كما يظهر من الكود

| البُعد | ما يقوله الكود |
|---|---|
| **البلد** | السعودية **حصراً**. جدول إدارات التعليم الـ١٦ (`js/edu-departments.js`)، وتقويم وزارة التعليم ١٤٤٨/١٤٤٩هـ بتقويمَيه (`js/academic-calendar.js:33`)، وشروط تحيل على «أنظمة وزارة التعليم» (`terms.html:160`) |
| **المرحلة** | الثلاث كلها — الصفوف الاثنا عشر مبنيّة صراحةً: ابتدائي · متوسط · ثانوي (`js/class-create.js:18-23`) |
| **الجنس** | **الاثنان، ولغتان**. نوع المدرسة (بنين/بنات) يقلب التطبيق كلَّه من «طالب» إلى «طالبة» عبر مصدر واحد (`js/words.js:1-46`) |
| **الشخصية** | المعلّم العادي لا التقني. ٣٠–٥٠ سنة، يستخدم واتساب وورد. مخاوفه الثلاثة: **التعقيد · فقدان البيانات · الإحراج أمام الطلاب** (`01_PRD:26-31`) |
| **المنافس الحقيقي** | **الورق + Word + واتساب** — لا Classera ولا مدرستي. نصّاً في `00_الملخص_التنفيذي.md:100` |
| **ما يدفع مقابله فعلاً** | تقرير جاهز بترويسة رسمية يُسلَّم للمدير/المشرف بضغطة (`RESEARCH_STUDENT_REGISTERS.md:126`) |

**منافسون مباشرون كُشفوا:** «المعلم الذكي» (١٢٠٩ تقييمات) · «المعلم الرقمي» · «سجل المعلم» · [mueen.io](https://www.mueen.io/) (٥٠٠٠ معلّم). **وكلاهما الأولان مغلقان على iOS — و«فصول» ويب يعمل على كل جهاز.**

---

## ٣) أقوى ستّ ميزات — مستخرجة من الكود الفعلي

### ① سجل المتابعة — التحضير في ثوانٍ، والخانات يصنعها المعلّم
**الملف:** `js/views/class.js` (٢٠٦٦ سطراً — أضخم شاشة في التطبيق)

- أربع حالات بضغطة: حاضر · غائب · متأخر · مستأذن — وضغط الحالة نفسِها يمسحها، فلا زرَّ تراجع (`class.js:28-36`)
- «✓ تحضير الكل» يحضّر من لم يُحضَّر ولا يمسّ من سُجّل غائباً؛ ثم ينقلب أحمرَ «✕ إلغاء تحضير الكل» بتأكيد (`class.js:529-635`)
- **خانات تقييم يصنعها المعلّم بنفسه** بأربعة أنواع: نجوم ٠-٥ · رقم بحدٍّ يختاره · علامة ✓/— · ثلاثيّ تم/جزئي/لم يتم (`class.js:40-47`, `1003`)
- التحضير المتأخّر: يفتح أيَّ يومٍ سابق ويكمله، والقائمة تريه أيَّ يومٍ نسيه (`class.js:63`, `357`, `491`)
- الأرقام العربية (٠-٩) تُقبل وتُحوَّل لحظة الكتابة (`class.js:93-106`)
- البحث بتطبيع عربي: التشكيل يسقط، و«إأآ»←«ا»، و«ة»←«ه» (`class.js:310-322`)
- التباين مقيس لا مقدَّر: ٦٫٥٣ · ٦٫٤٧ · ٧٫٠٩ · ٦٫٧٠ (`class.js:735`)

> **الوجع:** التحضير في الحصة يجب أن يُنجَز في ثوانٍ لا دقائق.

---

### ② ملف الإنجاز — ثلث الملف يكتب نفسه، ويخرج PDF بغلاف وفهرس
**الملفات:** `js/views/portfolio.js` (١٢٧٧ سطراً) · `js/print-portfolio.js` (١٠٣٤ سطراً)

- **عشرة أقسام** ثابتة بالترتيب المعروف، وثلاثة منها موسومة **«تلقائي»**: الاختبارات وأوراق العمل والواجبات تُسحب من فصول المعلّم بلا إدخال ثانٍ (`portfolio.js:47-63`, `214`, `1242-1270`)
- مكتبة **٣٠ صياغة** رسالة ورؤية وأهداف، كلٌّ موسومة بمحورها من المعايير المهنية للمعلمين (`js/mission-catalog.js:26-56`)
- **١٩ استراتيجية تدريس** بـ**٩٥ خطوة تنفيذية** — لا تعريفات نظرية (`js/strategies-catalog.js`)
- **٣٠ مبادرة مدرسية** في ٨ تصنيفات، بسياق سعودي (نافس · موهبة · يوم التأسيس) وقائمة «شواهد مقترحة» تقول ماذا يصوّر (`js/initiatives-catalog.js`)
- زرّ «✓ طبّقتها» يسجّل التاريخ والملاحظة والصور، فتتجمّع وحدها في الملف (`js/views/class-strategies.js:80-286`)
- **الطباعة:** غلاف رسمي + **فهرس بأرقام صفحات تُقاس فعلياً بعد الترقيم لا تُقدَّر** + صفحة فاصلة لكل قسم + شريط تقدّم «الصفحة ٣ من ٢٨» (`print-portfolio.js:86-129`)

> **الوجع:** مجلّد ورقي يُجمَع في آخر ليلة قبل زيارة المشرف.

---

### ③ ورقة اختبار بالتصميم الرسمي السعودي — ولا يُقصّ سؤالٌ بين صفحتين
**الملفات:** `js/question-editor.js` · `js/print-exam.js` (٧٥٤ سطراً)

- معالج **خطوتين لا أكثر**: اكتب ← اطبع (`class-exams.js:159-202`)
- **خمسة أنواع أسئلة**: اختيار من متعدد · صح/خطأ · أكمل الفراغ · مقالي · مطابقة — والنوع يُختار مرّةً للقسم كلِّه (`question-editor.js:55-64`)
- ترويسة «المملكة العربية السعودية / وزارة التعليم / إدارة التعليم بـ… / اسم المدرسة» مع شعار المدرسة (`print-exam.js:441-482`)
- **الترقيم يقيس بمسطرة حقيقية**: السؤال مع خياراته كتلة واحدة، وعنوان القسم لا يُترك وحيداً في ذيل الصفحة، وجدول صح/خطأ يُعيد رأسه ويكتب «تابع: السؤال الثاني» (`print-exam.js:530-592`)
- نموذج إجابة في صفحة منفصلة للمعلّم (`print-exam.js:401-423`)
- عمود المطابقة يُخلط **خلطاً ثابتاً** محسوباً من المعرّفات — فالنموذج يطابق الورقة كلَّما أُعيدت الطباعة (`print-exam.js:285-311`)
- **ورقة إنجليزية كاملة تلقائياً** لمعلّم الإنجليزي: العناوين والاتجاه وحروف A B C D والأرقام اللاتينية — بلا إعداد (`print-exam.js:72-129`)
- حارس يمنع طباعة ورقة ناقصة برسالة تسمّي القسم ورقم الفقرة (`question-editor.js:483-495`)

> **الوجع:** سؤالٌ في صفحة وخياراته في التي بعدها — أكثر ما يُفسد ورقةً مكتوبة في وورد.

---

### ④ صوّر جدولك — تُبنى فصولك وحصصك معاً
**الملف:** `js/views/schedule.js` (١٦٧٤ سطراً)

- يصوّر جدوله المطبوع أو يرفع PDF فتُقرأ الحصص وتُملأ الشبكة (`schedule.js:476-598`)
- **قاعدة حاكمة: لا يُكتب شيء قبل أن يراه المعلّم** — النتيجة تُعرض شبكةً يعدّل فيها خانةً خانة قبل «اعتمد»
- جدول مدرسة فيه معلّمون كثر؟ تُقرأ أسماؤهم **من طبقة نصّ الملف على الجهاز بلا أي استهلاك**، ويُسأل «أيّهم أنت؟»
- الفصول التي لا يملكها تُنشأ له تلقائياً، ويُقال له كم خانة ستُستبدل قبل الاعتماد
- وللأسماء: **إكسل يُقرأ داخل المتصفح** بـ`DecompressionStream` + `DOMParser` — بلا مكتبة، وبلا خروج الملف، وبلا ذكاء اصطناعي (`js/sheet-read.js`)
- ثم **شاشة مراجعة إلزامية** قبل الحفظ، ولافتة تقول المصدر بصدق: «قُرئت من الجدول خليّةً خليّة، بلا ذكاء اصطناعيّ» أو «قُرئت بالذكاء الاصطناعيّ — وقد يخطئ في الألقاب» (`class.js:1578-1707`)

> **الوجع:** أكبر عائق أمام أي تطبيق للمعلّم هو إدخال البيانات أوّل مرة.

---

### ⑤ يعمل بلا إنترنت — وصندوقٌ صادرٌ لا يضيع منه شيء
**الملفات:** `sw.js` · `js/database.js` (١٥٨٧ سطراً) · `js/app.js`

- معالج خدمة يخزّن التطبيق كلَّه، **ومكتباته محلية لا من CDN** فيُقلع أصلاً بلا شبكة (`index.html:311`)
- **صندوق صادر**: كل كتابة لا تصل الخادم تُصفّ وتُعاد عند عودة الشبكة (`database.js:1345-1375`)
- الرسالة تُقال **مرّةً واحدة** لا مع كل ضغطة: «لا اتصال — ما تكتبه محفوظٌ على جهازك، ويُرسل حين تعود الشبكة» ثم «عاد الاتصال — حُفظ كلُّ ما كان معلّقاً» (`app.js:233-238`)
- انقطاع الشبكة **لا يُخرجك من حسابك**: يُفرَّق بين رفض الخادم وسقوط الشبكة (`auth.js:690-726`)
- والخط مستضاف محلياً فلا يتغيّر شكل التطبيق كلُّه حين تنقطع الشبكة (`css/fonts.css`)

> **الوجع:** معلّم يرصد ثلاثين طالباً في فصل بلا تغطية، ثم يكتشف أن شيئاً لم يُحفظ.

---

### ⑥ يومك كما هو — التقويم الرسمي والجرس وحصتك القادمة
**الملفات:** `js/views/dashboard.js` · `js/academic-calendar.js` · `js/bell.js`

- الرئيسية **ستّ حالات لا شاشة واحدة**: بلا فصول · بلا جدول · جمعة/سبت · إجازة رسمية باسمها · يوم بلا حصص · يوم فيه حصص (`dashboard.js:559-589`)
- **تقويمان رسميّان** ١٤٤٨/١٤٤٩هـ: مكة والمدينة وجدة والطائف (١٨ أسبوعاً) وبقيّة المناطق (١٩) — يُختار **تلقائياً من إدارة تعليم المعلّم** (`academic-calendar.js:33-72`)
- بطاقة سطر واحد: «الأسبوع الثامن · إجازة الخريف بعد ٥ أيام»
- **جرس المدرسة** ثلاث ضربات + **تنبيه حصتك أنت** قبل ٣/٥/١٠/١٥ دقيقة — صوتان مولَّدان في المتصفح بلا ملفات، فيعملان بلا إنترنت (`bell.js:24-99`)
- «حصتك القادمة بعد ١٢ دقيقة» وتحتها زرّ يفتح **سجل المتابعة** لذلك الفصل مباشرة (`dashboard.js:486-527`)
- التاريخ هجريّ بتقويم **أمّ القرى** صراحةً

> **الوجع:** «في أي أسبوع نحن؟» و«متى الإجازة؟» — سؤالان يوميّان يُبحث عنهما في صورة منتشرة في الواتساب.

---

### ⑦ (احتياطي) المعلّم يختار لون تطبيقه
`js/theme-color.js` + `js/views/settings.js:940-1160` — **١٤ عائلة لون × ٧ درجات**، والحدّ الأدنى للتباين **مفروض بالحساب لا بالذوق**: الأبيض فوق اللون ≥ ٧:١، والذهبي فوقه ≥ ٤٫٥:١. لا تذكرها أيّ وثيقة — ميزة تسويقية جديدة تماماً.

---

## ٤) ثلاث جمل تسويقية رئيسية

### أ — الوجع اليومي *(مُرجَّحة)*
> # كلُّ فصلٍ في جيبك.
> ### التحضير والدرجات والاختبارات وملفُّ الإنجاز — في تطبيقٍ واحد، يعمل ولو انقطعت شبكة المدرسة.

**لماذا:** تبدأ من اسم التطبيق نفسه، وتذكر الوجع الأشهر (الشبكة) في نصف سطر. قصيرة وتصلح لعنوانٍ ضخم يظهر كلمةً كلمة.

---

### ب — الوعد النهائي
> # وثِّق وأنت تعمل، لا وأنت تُسأل.
> ### ملفُّ إنجازك يبني نفسه من حصصك — ويخرج مستنداً رسمياً بغلافٍ وفهرسٍ بضغطة.

**لماذا:** تصف أثمن ما في التطبيق فعلاً — الأقسام الثلاثة «تلقائي». وهي أقوى ما يميّزه عن سجلات الحضور المجرّدة.

---

### ج — التموضع ضد المنافس الحقيقي
> # لا دفترٌ ورقيّ، ولا ملفُّ وورد.
> ### سجلُّ المعلّم السعوديّ كما ينبغي أن يكون — بتقويم وزارتك، وترويسة مدرستك، وبخطٍّ عربيٍّ يُقرأ.

**لماذا:** المنافس مكتوبٌ في وثائقك: «الورق + Word + واتساب». هذه تسمّيه وتضربه.

---

## ٥) الألوان والخطوط المستخدمة اليوم

> ⚠️ `teacher_app/README.md` **قديم** — يذكر `#1E40AF` وخط Tajawal وIndexedDB. **كلُّها متجاوَزة.** التعليق في `index.html:7` يقول نصّاً إن الأزرق الملكي «لا وجود له في التطبيق».

### الألوان

| الرمز | القيمة | الدور | المصدر |
|---|---|---|---|
| **`--pf-royal`** | **`#0A3F4A`** | **لون الهوية — البترولي** | `css/portfolio-theme.css:19` |
| `--pf-royal-light` | `#14606F` | درجة فاتحة | |
| `--primary-mid` | `#105260` | أعلى التدرّج | |
| `--primary-deep` | `#05262D` | أسفل التدرّج | |
| **`--pf-gold`** | **`#C9A961`** | **الذهبي الفاخر — اللمسة** | `portfolio-theme.css` |
| `--pf-gold-light` | `#E8D9A8` | | |
| `--pf-charcoal` | `#2C2C2A` | الحبر | |
| `--pf-ivory` | `#F8F6F0` | العاج (مُحيَّد في المنشور) | |
| `--success` | `#10B981` | | `main.css` |
| `--warning` | `#F59E0B` | | |
| `--danger` | `#EF4444` | | |

**المظهر الأساسي: أبيض** (`settings.js:1637`) — أرضية `#FFFFFF`، حدّ `#E4E6EA`، نص `#2C2C2A`.
**الوضع الداكن** لوحة مبنيّة لا مقلوبة: `--bg #0D1117` · `--surface #161C27` · `--text #E8ECF3` (١٣٫٤:١) — **والبترولي هو هو في المظهرين** بطلبك (`theme-dark.css:50-57`).

> اقتباس ثمين من `theme-dark.css:31`: «الأسطح: أربعُ درجاتٍ بميلٍ كحليّ خفيف — لا رماديٌّ ميّت ولا أسودُ حادّ. **والأسودُ الخالص يُجهد العين** على الشاشات المضيئة.»
> و`theme-dark.css:40`: «الحبر: أبيضُ مكسورٌ لا خالص — **الخالصُ يهتزّ على الداكن (halation)**.»

**هذان الاقتباسان يحكمان لوحة صفحة الهبوط:** لا أسود خالص `#000`، ولا أبيض خالص `#FFF`.

### الخط

**IBM Plex Sans Arabic** — مستضاف محلياً في `teacher_app/vendor/fonts/`، رخصة SIL OFL 1.1.

```
'IBM Plex Sans Arabic', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
```

**أربعة أوزان لا خامس: ٤٠٠ · ٥٠٠ · ٦٠٠ · ٧٠٠.**

> 🚫 **قيدٌ صارم** من `main.css:102`: «لا `--fw-black`: العائلةُ تنتهي عند ٧٠٠… وكان الرمزُ ٩٠٠ فيُطلَب في ١٦٧ موضعاً، **فيزوّره المتصفّح**: يمطّ حروفَ الـ٧٠٠ بتغليظٍ صناعيٍّ بلا خطٍّ خلفها، فتخرج غليظةً مشوّهةً على الشاشات الكثيفة.»
> **صفحة الهبوط لن تستعمل ٨٠٠ ولا ٩٠٠ إطلاقاً** — العناوين الضخمة بـ٧٠٠.

### السلالم — فيبوناتشي

| | |
|---|---|
| **المسافات** | `3 · 5 · 8 · 13 · 21 · 34 · 55 · 89` |
| **الطباعة** | display 42 · h1 34 · h2 26 · lg 21 · body 16 · sm 13 · xs 11 (كل درجة ≈ ×1.272 = √φ) |
| **ارتفاع السطر** | tight `1.236` · base `1.618` (φ) |
| **الأقطار** | 8 · 13 · 21 · 34 |
| **الحركة** | `--ease: cubic-bezier(0.4, 0, 0.2, 1)` · **144ms · 233ms · 377ms** (فيبوناتشي) |

### الأيقونات
**٦٣ رسماً SVG خطّياً** في `js/icons.js` — `viewBox 0 0 24 24` · `stroke-width 1.8` · `fill none` · `stroke currentColor` · `linecap/linejoin round` · **مسار واحد لكل رمز**. سأتبع النمط نفسه في الصفحة.

### الشعار
لا ملف صورة. الشعار الوحيد **SVG داخلي** في `index.html:101-115`: **قبعة تخرّج داخل قرص بتدرّج بترولي وحلقة ذهبية `#C9A961` وشرّابة**. سأعيد رسمه بالقيم نفسها.

---

## ٦) 🚫 ما لا يُقال في الصفحة — خطوط حمراء

الوكلاء العدائيّون كشفوا فجوات بين توثيقك وكودك. **هذه ادّعاءات ستكشفك أمام أول معلّم:**

| ❌ لا يُقال | ✅ الصحيح | الدليل |
|---|---|---|
| «الذكاء الاصطناعي يولّد اختباراتك من كتابك» | **الإنشاء يدويٌّ بالكامل**، والذكاء محصور في موضعين: قراءة الجدول وقراءة كشف الأسماء | `ai-service.js:169,192` · `class-exams.js:58` |
| «وفّر ٥ ساعات أسبوعياً» | **رقم لا يقابله قياس في أي ملف** | — |
| «كل شيء على جهازك، لا خادم» | Supabase قاعدةً ومصادقة. **الصادق:** «كتبك لا تغادر جهازك» و«كشف أسمائك يُقرأ داخل المتصفح» | `README.md:8` · `sheet-read.js:8` |
| «إشعارات تصلك والجوال في جيبك» | المنبّه يعمل **والتطبيق مفتوح فقط** — مكتوب صراحةً في الكود | `bell.js:4-8` |
| «تصدير متوافق مع نور» | **لا وجود لكلمة «نور» في أي ملف JS** | — |
| ذكر `#1E40AF` أو خط Tajawal | البترولي `#0A3F4A` وIBM Plex Sans Arabic | `portfolio-theme.css:19` |

**وميزات خلف مفاتيح معاينة — ليست مشحونة:** الثوب الزجاجي `?calm=1` (`css/theme-calm.css`). لن تظهر في الصفحة.

**وجملة ذهبية جاهزة للاقتباس الحرفي** من `terms.html:178` — أقوى نازعٍ للخوف في المشروع كلّه:
> **«وإن انتهى اشتراكك، لا تُحذف بياناتك؛ تبقى ويقلّ ما تستطيع فعله بها حتى تجدّد.»**

---

## ٧) القرارات — أُخذت ٣ سبتمبر ٢٠٢٦

| # | القرار | الاختيار |
|---|---|---|
| **١** | **المكان والنشر** | **أ** — مجلّد `landing/` مستقلّ داخل هذا المستودع، Next.js 14 بـ`output: 'export'`، ثم نشر على **Vercel** على نطاق `fusooli.com`. **لا يُلمس `teacher_app` ولا `deploy.yml`.** |
| **٢** | **اللون المميّز** | **ج — الاثنان معاً**: البترولي `#0A3F4A` للتوهّج (glow) والمساحات، والذهبي `#C9A961` للحدود والتفاصيل الدقيقة. *(يخالف قاعدة «لون واحد» في الطلب الأصلي — مقصودٌ ومُقرّر. والانضباط يأتي من الندرة: الذهبي في مواضع معدودة لا في كل حدّ.)* |
| **٣** | **موك أب الجوال** | **أ — لقطات حقيقية**: أشغّل التطبيق محلياً وألتقط شاشات فعلية (الرئيسية · سجل المتابعة · الجدول · ملف الإنجاز · ورقة الاختبار). |
| **٤** | **زرّ CTA الأخير** | **ب — شارات المتاجر معطّلة** مع «قريباً». ⚠️ **قيدٌ يجب مراعاته:** إرشادات آبل التسويقية تمنع استعمال فنّ شارة App Store الرسمي لتطبيق لم يُنشر بعد. فالشارتان تُرسمان **بأسلوبنا نحن** (شكل الشارة المعروف بخطّنا وألواننا، بلا شعار آبل الرسمي ولا فنّ قوقل بلاي) وعليهما «قريباً» صريحةً — فيبقى الشكل المألوف بلا مخالفة. |

### ما زال بلا قرار (غير حاجب — لي فيه توصية أمضي بها ما لم تعترض)

| # | البند | ما سأفعله |
|---|---|---|
| **٥** | **قسم الأرقام (السادس)** — لا مستخدمين بعد، فلا يُكتب «٥٠٠٠ معلّم» | أرقام **من داخل المنتج**، كلُّها مقيسة من الكود: ١٩ استراتيجية · ٣٠ مبادرة · ١٠ أقسام · ٥ أنواع أسئلة · تقويمان رسميّان · ٩٥ خطوة تنفيذية |
| **٦** | **السعر** | **لا سعر في الصفحة.** تُذكر «فصلك الأول مجاناً» فقط — وجملة `terms.html:178` الذهبية: «وإن انتهى اشتراكك، لا تُحذف بياناتك» |

---

## ٨) ما سيأتي بعد موافقتك

1. `DESIGN.md` — لوحة الألوان، سلّم الطباعة، منطق المسافات، فلسفة الحركة → **موافقتك**
2. تجهيز `landing/` (Next.js 14 + TS + Tailwind + Framer Motion + Lenis)
3. بناء **قسماً قسماً** مع عرض النتيجة بعد كل قسم رئيسي

---

*آخر تحديث: ٣ سبتمبر ٢٠٢٦ · مبنيّ على `ef6f7dc` · لم يُعدَّل أي ملف من التطبيق.*
