# PLAN.md — حالة مشروع تطبيق المعلم الذكي

> آخر تحديث: 2026-05-13 (فحص شامل بوضع آمن — بدون تعديل كود)

---

## ١. وين وصلنا (نسبة الإنجاز التقريبية: ~85%)

### ✅ منجز ويعمل

| الميزة | الحالة |
|---|---|
| البنية الأساسية (Supabase + GitHub Pages + Service Worker) | مكتمل |
| تسجيل الدخول / حساب زائر | مكتمل |
| الفصول (إضافة/عرض/تحديث فوري) | مكتمل |
| سجل الطلاب (حضور + تقييم + أعمدة مخصصة + عمود مثبّت) | مكتمل |
| استيراد الطلاب (لصق / CSV / صورة-PDF بالذكاء الاصطناعي) | مكتمل |
| الجدول الأسبوعي (أيام يمين × حصص فوق + حصة انتظار + استيراد من صورة) | مكتمل |
| الملف التعريفي (حفظ موحّد + حقل المنطقة) | مكتمل |
| ملف الإنجاز — ثيم ملكي (أزرق #0F2C5C + ذهبي #C9A961) | مكتمل |
| ملف الإنجاز — بطاقة شخصية حكومية (شاشة + طباعة) | مكتمل |
| ملف الإنجاز — غلاف رسمي + فهرس بأرقام صفحات + فواصل أقسام | مكتمل |
| ملف الإنجاز — بطاقات الشهادات والجداول (صفحة/ملف) | مكتمل |
| ملف الإنجاز — أقسام مخصصة غير محدودة | مكتمل |
| الكتب: حفظ PDF محلياً (IndexedDB بلا حد) + استخراج النص تلقائياً | مكتمل (غير مختبر ميدانياً) |
| إقرار ملكية الكتاب + آلية إزالة (TAKEDOWN.md) | مكتمل |
| بروكسي Anthropic عبر Edge Function (مفتاح مشترك آمن) | مكتمل ويستجيب |
| توليد الاختبارات مع التزام صارم بالمادة | مكتمل |

### ⏳ متبقٍ من الخطة (~15%)

1. **تصاميم طباعة ملف الإنجاز للأقسام 5–10** (الاختبارات، أوراق العمل، الواجبات، الاستراتيجيات، المبادرات، المرفقات) — توقفنا عند القسم 4.
2. **مشكلة اقتطاع سياق الكتاب** (انظر المشاكل أدناه — الأخطر حالياً).
3. **فكرة المكتبة المركزية / RAG** — مؤجلة في IDEAS.md.
4. OCR للكتب الممسوحة ضوئياً — غير مبدوء.
5. cron لإبقاء Supabase نشطاً — عُرض ولم يُنفَّذ.

---

## ٢. حالة المشروع الآن

| الفحص | النتيجة |
|---|---|
| صياغة 32 ملف JavaScript (`node --check`) | ✅ كلها سليمة |
| GitHub Pages | ✅ 200 |
| Supabase REST | ✅ يستجيب (401 طبيعي بدون توكن) |
| Edge Function `anthropic-proxy` | ✅ 204 |
| آخر 3 نشرات GitHub Actions | ✅ نجحت كلها |
| المهاجرات المحلية ↔ البعيدة (13) | ✅ متزامنة |
| شجرة git | ✅ نظيفة على `main` |

**لا توجد أخطاء بناء أو صياغة. التطبيق يعمل.**

---

## ٣. المشاكل الحالية (من الأخطر للأبسط)

1. **🔴 سياق الكتاب يُقتطع إلى 8000 حرف فقط**
   - الملف: `teacher_app/js/ai-service.js` سطر 337 — `opts.context.slice(0, 8000)`
   - استخراج نص الكتاب كاملاً يعمل، لكن التوليد يرسل أول ~5 صفحات فقط. طلب "الفصل التاسع" لن يصل محتواه للنموذج أبداً.
   - الحل المقترح: بحث عن الفصل المطلوب داخل النص (عبر علامات `[صفحة N]`) أو رفع الحد بذكاء.

2. **🟠 مفتاح Anthropic المكشوف سابقاً ما زال مستخدماً**
   - المفتاح الذي لُصق في المحادثة محفوظ في أسرار Supabase ويشغّل البروكسي.
   - المطلوب: إنشاء مفتاح جديد من console.anthropic.com وتحديث السر (`supabase secrets set`).

3. **🟠 نص الكتاب الكامل يُحفظ في عمود `context` بجدول books**
   - كتاب 300 صفحة ≈ 0.5–1 MB نص يُسحب مع كل hydrate عند تسجيل الدخول → إبطاء تدريجي.
   - الملف: `teacher_app/js/views/class-books.js` (الاستخراج) + `database.js` (hydrate).

4. **🟡 مشروع Supabase المجاني يتوقف بعد 7 أيام خمول**
   - حدث فعلاً مرة. الحل الدائم: cron أسبوعي أو خطة Pro.

5. **🟡 الكتب الممسوحة ضوئياً (صور) يخرج استخراجها فارغاً**
   - لا يوجد OCR. المعلم لن يفهم لماذا الأسئلة عامة رغم رفع الكتاب.

6. **🟢 ملف تجريبي منشور مع التطبيق**
   - `teacher_app/cert-designs.html` (معاينة تصاميم قديمة) يُنشر على الموقع العام — يُحذف.

7. **🟢 مرفقات ملف الإنجاز المرفوعة قبل إصلاح base64 فقدت محتواها**
   - تظهر رسالة "يُرجى إعادة رفعه" في الطباعة — سلوك مقصود، يحتاج فقط إعادة رفع يدوية.

---

## ٤. آخر شيء تم الشغل عليه

آخر 3 commits (كلها 2026-05-12):

```
3ac3ce3 feat(books): استخراج نص الـPDF تلقائياً ليستعمله Claude
e15010d feat(books): إقرار ملكية المعلم + آلية إزالة الكتب
b85c3bb fix(exams): التزام صارم بمادة الفصل + تحذير من السياق الفاضي
```

المحور: جعل توليد الاختبارات يعتمد على نص الكتاب الحقيقي (رفع محلي → استخراج → سياق للنموذج) مع الضمانات القانونية.

---

## ٥. الخطوة الجاية المقترحة (مهمة واحدة)

**إصلاح اقتطاع سياق الكتاب (المشكلة #1):**
عند توليد اختبار عن موضوع/فصل محدد، ابحث في نص الكتاب المستخرج عن الجزء المطلوب (باستخدام علامات `[صفحة N]` وكلمات الموضوع) وأرسل ذلك المقطع (~30-50 ألف حرف) بدل أول 8000 حرف أعمى.

- **لماذا هي الأولى:** بدونها ميزة "الـAI يقرأ الكتاب" — التي بُني عليها آخر أسبوع عمل — لا تعمل فعلياً إلا لأول درسين من الكتاب.
- **الملفات:** `ai-service.js` (المنطق) + `class-exams.js` (تمرير الموضوع).
- **الحجم:** ~ساعة عمل.

---

## مراجع

- التراجع لنسخة مستقرة: `RECOVERY.md` (وسوم: v1.0-stable، v1.1-portfolio-colors)
- سجل الجلسات: `SESSIONS.md`
- أفكار مؤجلة (مكتبة مركزية/RAG): `IDEAS.md`
- إزالة كتاب بطلب قانوني: `TAKEDOWN.md`
- تقارير: `PORTFOLIO_CURRENT_DESIGN.md`، `PROJECT_HOSTING.md`
