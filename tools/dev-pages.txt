# ملفّاتُ التطوير — تبقى على جهاز المطوّر ولا تخرج معه.
# (صفحاتٌ وأدواتٌ ووثائق — كلُّ ما ليس للمعلّم.)
#
# **مكانٌ واحدٌ يقرؤه اثنان**: خطوةُ النشر في .github/workflows/deploy.yml،
# وأمرُ التغليف حين يُبنى. ولولا ذلك لصارت القائمةُ في موضعين — وأوّلُ أداةٍ
# جديدةٍ تُنسى في أحدهما، فتُنشر من حيث لا نشعر.
#
# ولماذا تُستثنى أصلاً: أربعٌ منها تتّصل بالقاعدة الحقيقيّة و**تُنشئ حسابَ
# زائرٍ وتكتب صفوفاً لكلّ من يفتحها**، وكلُّها تُحزَم مع التطبيق عند التغليف
# فيراها مراجعُ آبل. (فحصُ ٢٧ أغسطس ٢٠٢٦ — انظر كوميت 5e7879f.)
#
# القواعد: سطرٌ لكلّ ملفّ، بمسارٍ نسبيٍّ من teacher_app/.
# ما بدأ بـ# تعليقٌ يُتجاهل، والأسطرُ الفارغةُ كذلك.
# ولا تضع هنا صفحةً يحتاجها المعلّم (index/privacy/terms).

eval.html
contrast.html
smoke.html
diag-print.html
reset.html   # صفحةُ إنعاش الشاشة البيضاء — **قرارُه ٩ سبتمبر: تُحذف**.
             # وكانت خطّةُ الاختبار تريدها منشورةً بابَ إنقاذ. وثمنُ الحذف
             # مقيس: معلّمٌ يتعطّل مخزنُه فيرى شاشةً بيضاء **لا يجد باباً**،
             # ويلزم شرحُ مسح بيانات الموقع من إعدادات المتصفّح عبر الدعم.
             # ولا يُعاد نشرُها إلّا بكلمته.
fingerprint.html

# أُضيفتا ٣٠ أغسطس ٢٠٢٦ — وكانتا تُنشران وقد فات إدراجُهما:
free.html      # فحصُ القراءة المجّانية من PDF — أداةُ قياسٍ لا شاشةُ معلّم
stg.html       # معاينةُ شكل قسم الاستراتيجيات — عُرضت وانتهى أمرُها
btns.html

# أُضيفت ٩ سبتمبر ٢٠٢٦ — البند ١٫٥ في خطّة الاختبار:
isolation.html   # فحصُ العزل بين حسابين — يُنشئ حسابين ويكتب صفوفاً
datacycle.html   # فحصُ دورة البيانات — يقود أزرارَ الواجهة ويُنشئ حساباً
journeys.html    # فحصُ الرحلات — من التهيئة إلى أوّل تحضير، ويُنشئ حساباً
security.html    # فحصُ الأمن — يزرع حمولاتِ حقنٍ ويُنشئ حساباً

# أُضيفت ١٠ سبتمبر ٢٠٢٦ — البوّابة ٤ب:
printing.html    # فحصُ الطباعة — يقود أزرارَ التصدير ويقرأ الملفّات بـpdf.js

# أُضيفت ٤ سبتمبر ٢٠٢٦ — ليست صفحاتٍ لكنّها تُشحن مع الحزمة ويراها المراجع:
README.md                    # وثيقةُ المطوّر
tools/check-fixed-surfaces.mjs   # حارسُ الأسطح المثبَّتة — يُشغَّل من المستودع
tools/check-active-states.mjs    # حارسُ الحالات الملوّنة — يُشغَّل من المستودع
tools/check-version.mjs          # حارسُ رقم الإصدار — أُضيف ٥ سبتمبر ٢٠٢٦
tools/check-assets.mjs           # حارسُ الأصول — أُضيف ٩ سبتمبر ٢٠٢٦
